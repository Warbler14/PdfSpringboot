한자 pdf 파일 읽고 데이너 맵을 추출하는 라이브러리, 과정에서 db 인서트 쿼리 실행 요청도 할 수 있도록 수정

sqlit 파일 생성 후 xml 읽어서 테이블 생성하는 코드, 라이브러리로 만들어져야 한다.

pdf 파일 읽고 형식에 따라 데이터 읽은 후 sqlite 파일 생성하고 자료 저장하는 코드, 라이브러리로 만들어져야 한다.

mybatis 로 sqlite 파일을 읽고 데이터를 입출력 하는 코드, 라이브러리로 만들어져야 한다.

스프링부트에서 여러개의 sqlite 파일 관리 라이브러리 호출

ehcache change version